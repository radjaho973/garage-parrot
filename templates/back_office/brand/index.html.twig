{% extends '/back_office/base_back_office.html.twig' %}

{% block title %}Gestion des Marques{% endblock %}

{% block body %}
<div class="col-9 ">
    <h1>Gestion des Marques</h1>
    <a class="btn btn-primary" href="{{ path('app_brand_new') }}">Create new</a>
    <table class="table">
        <thead>
            <tr>
                <th>Brand</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for brand in brands %}
            <tr>
                <td>{{ brand.brand }}</td>
                <td>
                    <a href="{{ path('app_brand_edit', {'id': brand.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Pas de donnée</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
